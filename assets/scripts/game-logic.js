//let player = "x"
//html divs are turned into an array


let game = ["", "", "", "", "", "", "", "", ""]




const checkGame = function(game) {
  for(let i = 0; i < game.length; i++) {
    if(game[i] === "")
  }
  return true;
}*/

//const clickedCell = function(event) {
//the player selects a cell
//if the player selects a cell on the first turn that cell is filled with X
//the player alternates between 'o' and 'x' after the first turn
//the game ends if they win
//they win by having a row filled with 'Xs' or 'Os'
//they win by having a column filled with 'Xs' or 'Os'
//they win by having a diagonal filled with 'Xs' or 'Os'
}
//$('#message').text("That choice isn't available, try again")
